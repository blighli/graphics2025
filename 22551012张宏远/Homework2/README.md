# MyExecutable

一个基于 **CMake** 构建的 C++ 项目，用于生成图像并将结果输出为 **PPM** 格式文件。

------

## 📦 构建环境要求

- **CMake** ≥ 3.15
- 支持 C++ 的编译器（如 MSVC / GCC / Clang）
- 操作系统：Windows（以下示例基于 Windows）

------

## 🚀 构建步骤

在项目根目录下执行以下命令：

```
cmake -B build
cmake --build build
```

上述命令将会：

1. 在 `build/` 目录中生成构建文件
2. 编译项目并生成可执行文件

------

## ▶️ 运行程序

构建完成后，在 Windows 下运行 Debug 版本可执行文件，并将输出重定向为图像文件：

```
.\build\Debug\MyExecutable.exe > image.ppm
```

------

## 🖼 输出说明

- 程序会将图像数据输出到标准输出（stdout）
- 通过重定向生成 `image.ppm` 文件
- 生成的 `image.ppm` 可使用以下方式查看：
  - 支持 PPM 的图像查看器
  - ImageMagick
  - GIMP
  - 自行编写脚本转换为 PNG / JPG

------

## 📁 项目结构示例

```
.
├── CMakeLists.txt
├── src/
│   └── main.cpp
├── build/
│   └── Debug/
│       └── MyExecutable.exe
└── image.ppm
```

------

## 📌 备注

- 如果你使用的是 **Release** 模式，请将路径改为：

  ```
  .\build\Release\MyExecutable.exe > image.ppm
  ```

- 可根据需要在 `CMakeLists.txt` 中调整构建选项